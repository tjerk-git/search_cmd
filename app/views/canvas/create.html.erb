<div class="large">
<%= form_with(model: @canvas) do |form| %>
  <div class="canvas_form">
    <h1>You can create your own CMD PPO canvas here!</h1>
    <span class="red">Canvas can only be found by its unique URL, if you lose that URL, its gone</span>
    <%= form.text_field :name, placeholder: "Your name please" %>
    <%= form.submit "Create personal canvas", id: "canvas_form_submit" %>
  </div>
<% end %>


<% if @canvas.errors.any? %>

   <h1><%= pluralize(canvas.errors.count, ‘Error’) %> 
   <% canvas.errors.full_messages.each do |error| %></h1>
     <h2><%= error %></h2>
   <% end %>
  <% end %>
</div>